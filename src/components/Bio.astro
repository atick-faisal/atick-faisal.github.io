---
/**
 * Bio Section Component
 *
 * Displays personal introduction with background image, profile photo, and CTA button.
 * Background images switch based on dark/light mode.
 */
import { Image } from 'astro:assets';
import { Mail } from 'lucide-astro';
import profileImage from '../assets/atick.webp';
import { ICON_SIZES } from '../constants/ui';
---

<section id="about" class="container mx-auto px-6 py-16">
    <div class="bio-header relative rounded-header overflow-hidden">

        <!-- Content -->
        <div class="relative z-10 flex flex-col md:flex-row items-end">
            <!-- Left side - Text content -->
            <div class="description flex-[1.5] space-y-6 px-6 py-8 md:px-12 md:py-12">
                <h2 class="text-gray-900 dark:text-white">
                    Hello! I'm<br />
                    <span class="text-gray-900 dark:text-white">Atick Faisal</span>
                </h2>
                <p class="text-gray-800 dark:text-gray-200">
                    I'm a Data Scientist, Computer Vision Researcher, and Software Developer. 
                    I currently work as a Data Scientist at Iberdrola and am pursuing a Ph.D. in Computer Vision at Qatar University.
                </p>
                <div>
                    <a
                        href="#footer"
                        class="btn btn-primary inline-flex items-center gap-2"
                    >
                        <Mail size={ICON_SIZES.MD} />
                        Get in Touch
                    </a>
                </div>
            </div>

            <!-- Right side - Profile image -->
            <div class="flex-1 self-end">
                <Image
                    src={profileImage}
                    alt="Atick Faisal"
                    class="image w-full max-w-full rounded-3xl -scale-x-100 block"
                    widths={[200, 400, 500]}
                    sizes="(max-width: 800px) 100vw, 500px"
                    width={500}
                    height={500}
                />
            </div>
        </div>
    </div>
</section>

<style>
    .bio-header {
        background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 50%, #90caf9 100%);
    }

    :global(.dark) .bio-header {
        background: linear-gradient(135deg, #0d47a1 0%, #1976d2 50%, #2196f3 100%);
    }
</style>
