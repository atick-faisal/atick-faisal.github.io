/**
 * UI Constants
 * Centralized constants for consistent UI sizing and spacing across the application.
 */

export const ICON_SIZES = {
    SM: 16, // Small icons (card buttons)
    MD: 20, // Medium icons (navbar, buttons, footer)
    LG: 24, // Large icons (if needed)
} as const;

export const SELECTOR = {
    MIN_WIDTH: 140, // Minimum width for selector buttons (px)
} as const;
